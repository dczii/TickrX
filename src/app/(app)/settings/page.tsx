import SectionPlaceholder from "@/components/layout/SectionPlaceholder";

export default function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Settings"
      description="Preferences, notifications, and account controls."
      comingIn="Phase 5.5"
    />
  );
}
